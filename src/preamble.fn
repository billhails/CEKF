let
    typedef bool { true | false }

    typedef list(#t) { cons(#t, list(#t)) | nil }

    fn append {
        ([], b) { b }
        (h @ t, b) { h @ append(t, b) }
    }

    fn member {
        (_, []) { false }
        (x, x @ _) { true }
        (x, _ @ t) { member(x, t) }
    }

in {
