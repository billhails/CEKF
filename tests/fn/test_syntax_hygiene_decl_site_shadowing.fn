let
    helper = fn (n) { n + 1 };

    syntax callHelper: Expr ::= "callHelper" arg: Expr quote {
        helper(unquote(arg))
    };
in
    {
        let
            helper = fn (n) { n + 100 };
        in
            callHelper 41;
    }