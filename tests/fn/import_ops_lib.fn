namespace

fn negate(x) { 0 - x }
operator "~_" 13 negate;

fn plus(a, b) { a + b }
operator "_plus_" left 100 plus;

fn squared(x) { x * x }
operator "_squared" 120 squared;

// Export all operators defined in this namespace
export operators;
