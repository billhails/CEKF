let
    link "listutils.fn" as list;
    Σ = "abc\u03b1;\u03b2;\u03b3;";
    Ψ = "abcαβγ";
in
    assert(list.length(Σ) == 6);
    assert(list.length(Ψ) == 6);
    assert(Σ == Ψ);
    assert('\u03b1;' == 'α');
