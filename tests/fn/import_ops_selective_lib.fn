namespace

fn negate(x) { 0 - x }
export operator "~_" 13 negate;

fn plus(a, b) { a + b }
// Export only selected fixities
export operator "_plus_" left 100 plus;

fn squared(x) { x * x }
operator "_squared" 120 squared;

