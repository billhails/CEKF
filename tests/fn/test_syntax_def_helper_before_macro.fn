let
    syntax annotateHelper ::= name: Name "=" value: Expr {
        name = value
    };

    macro annotate: Def annotateHelper;

    annotate answer = 42;
    annotate ok = true;
in
    assert(answer == 42);
    assert(ok)