// Selective import of fixities from a linked namespace

let
    link "import_ops_selective_lib.fn" as ops;
    import ops operator "~_";
    import ops operator "_plus_";
in
    // Imported Prefix and Infix should work
    assert(~5 == -5);
    assert(3 plus 4 == 7);

