// Test that currentFile variable is available and has correct value

let
    // Test that currentFile is accessible
    fileIsAccessible = currentFile;
    
    // Test that it has the expected filename
    expectedFile = "tests/fn/test_currentfile.fn";
    
in {
    assert(currentFile == expectedFile);
}
