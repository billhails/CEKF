// Test: statements with multiple sequential semicolons
let
    fn id(x) { x }
    a = 1;
    b = 2;
    c = 3
in {
    assert(id("one") == "one");;
    assert(id("two") == "two");;;
    assert(id("three") == "three")
}
